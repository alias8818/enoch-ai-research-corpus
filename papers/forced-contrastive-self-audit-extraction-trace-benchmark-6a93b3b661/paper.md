# Forced Contrastive Self-Audit Extraction Trace Benchmark: Evidence-Grounded Technical Report

> **AI Provenance Notice:** This artifact was generated by an AI system from automated research artifacts. The operator claims no personal authorship credit for the writing or results beyond releasing the artifact. Readers should treat this as an unreviewed AI-generated research artifact. No independent human review has been performed on the code, results, or interpretation.

---


## Abstract

We report an evidence-grounded technical result from the OMX research automation pipeline for **Forced Contrastive Self-Audit Extraction Trace Benchmark**. The recorded project decision is `finalize_positive`, with hypothesis status `supported`, evidence strength `strong`, and confidence `high`. This draft is generated from project-local artifacts and is released as an unreviewed AI-generated research artifact for corpus inspection.

## 1. Introduction

This report summarizes the motivation, method, observed evidence, and limitations for Forced Contrastive Self-Audit Extraction Trace Benchmark.

## 2. Related Work

External citation expansion and comparison against published work are outside this packaging/provenance release gate.

## 3. Method

The method is reconstructed from the project prompt, run notes, and result artifacts.

## 4. Experimental Setup

- Project ID: `source-record-redacted`
- Run ID: `source-record-redacted-20260414025515`
- Decision source: `codex_turn`
- Evidence strength: `strong`
- Confidence: `high`

## 5. Results

### Key run-note evidence

- - Installed project-local runtime dependencies into `.venv` (`torch` CUDA 13.0 wheel, `transformers`, `accelerate`, `fastapi`, `uvicorn`) using `uv pip install --python .venv/bin/python ...`.
- - Found cached model snapshot `<local-path-redacted>`; verified it loads on CUDA with Transformers.
- - Added `scripts/openai_transformers_shim.py`, a minimal OpenAI-compatible `/v1/models` and `/v1/chat/completions` shim backed by the cached Transformers model.
- - Launched the shim from neutral directory `/tmp/fcsa_services` on `127.0.0.1:18000`, verified `/v1/models`, and verified `/v1/chat/completions` with a JSON test prompt.
- - First live run exposed a practical model-output issue: Qwen wrapped JSON in markdown fences, so `scripts/run_openai_compatible.py` recorded JSON parse errors. Updated the runner to tolerate fenced/extraneous JSON objects and preserve `raw_content` for auditability.
- - Reran the benchmark against `Qwen2.5-0.5B-Instruct`; wrote `results/model_outputs.jsonl` and scored it to `results/model_eval.json`.
- - Live score: `n=5`, `mean_score=0.5075`, `answer_correct=3/5`. Per-task scores: invoice `0.9000`, clinical dose `0.5750`, meeting owner `0.0625`, contract termination `0.2500`, shipping address `0.7500`.
- - Interpretation: the small cached model often extracts the answer but does not reliably provide the required structured contrastive audit trace (`audit_coverage=0.0` on all seeded tasks) and fails two distractor-heavy tasks by selecting the distractor.
- - Stopped the helper server; `127.0.0.1:18000` no longer responds.
- ## Validation after live run
- - Passed `.venv/bin/python -m py_compile src/fcsa_benchmark.py scripts/run_openai_compatible.py scripts/openai_transformers_shim.py`.
- - Passed `.venv/bin/python -m pytest -q` (`6 passed in 0.01s`).

### Result artifacts

- results/model_eval.json
- results/model_outputs.jsonl
- results/sample_eval.json
- results/prompts.jsonl

## 6. Discussion

The current project decision recommends: Use results/model_eval.json as the Qwen2.5-0.5B baseline and only reopen this mechanism to compare a stronger cached instruct model or calibrate the audit rubric.

The interpretation should remain bounded to the project artifacts and environment.

## 7. Limitations

- This draft is automatically generated for human review and must be checked before public release.
- Claims are limited to the project artifacts listed in the evidence bundle.
- External replication and broader hardware/model coverage are outside this packaging/provenance release gate unless explicitly present in the artifacts.
- Project stop/decision rationale: The benchmark harness was run against a verified local OpenAI-compatible endpoint, scored successfully, validated with tests, and the helper server was stopped.

## 8. Reproducibility Statement

The artifact manifest below identifies the project-local files used to construct this draft.

## 9. Conclusion

This draft captures the current evidence state for Forced Contrastive Self-Audit Extraction Trace Benchmark. It is suitable as a starting point for a human-authored public paper, not as a final submission.

## References

BibTeX/arXiv reference completion is outside this packaging/provenance release gate.

## Appendix A: Artifact Manifest

### Result files
- results/model_eval.json
- results/model_outputs.jsonl
- results/sample_eval.json
- results/prompts.jsonl

### Recent files
- .omx/metrics.json
- .omx/project_decision.json
- run_notes.md
- tests/test_fcsa_benchmark.py
- scripts/run_openai_compatible.py
- scripts/openai_transformers_shim.py
- .omx/project.json
- prompts/resume.md
- prompts/initial.md
- README.md
- src/fcsa_benchmark.py
- samples/sample_outputs.jsonl
